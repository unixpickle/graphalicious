//deps includes.js

function FullCurveChunkView(attrs, chunk, dataSource) {
  EventEmitter.call(this);
  
  // TODO: save more information here.
}

FullCurveChunkView.prototype = Object.create(EventEmitter.prototype);

// TODO: implement drawing, etc.
